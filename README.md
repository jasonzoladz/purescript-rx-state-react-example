# purescript-rx-state-react-example

An example of using [`purescript-rx-state`](https://github.com/jasonzoladz/purescript-rx-state) with `purescript-react`.

Just:

`npm install`

`bower install`

`npm start`

Point your browser to `http://localhost:8080`
